module.exports = ({ env }) => ({
  'import-export-entries': {
    enabled: true,
    config: {
      // See `Config` section.
    },
  },
  // bulkoperator: {
  //   enabled: true,
  //   resolve: "strapi-bulk-operator",
  // }
});